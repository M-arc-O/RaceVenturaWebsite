<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Stages and race end information for @Model.Name</title>
</head>
<body>
    <div class="row">
        <h1>Stages and race end information for @Model.Name</h1>
    </div>

    @foreach (var stage in Model.Stages)
    {
        <p>
            <div class="row mt-4">
                <h3>@Model.Name - Stage @stage.Number (@stage.Name) end information</h3>
            </div>

            <div class="row">
                <div class="col">
                    By scanning this QR code you will finish this stage for your team. You cannot scan any points for stage @stage.Number (@stage.Name) afterwards! To be able the scan point of the next stage you must end this one!
                </div>
                <div class="col">
                    <img class="qrcode" src="@String.Format("data:image/png;base64,{0}", Convert.ToBase64String(stage.QrCodeArray))" height="100" width="100" />
                </div>
            </div>
            <br />
            <br />
            <br />
        </p>
    }

    <p>
        <div class="row mt-4">
            <h3>@Model.Name end information</h3>
        </div>
        <div class="row">
            <div class="col">
                Scan this QR code to end the race. This will register your teams finish time.
            </div>
            <div class="col">
                <img src="@String.Format("data:image/png;base64,{0}", Convert.ToBase64String(Model.QrCodeArray))" height="100" width="100" />
            </div>
        </div>
    </p>
</body>
</html>
