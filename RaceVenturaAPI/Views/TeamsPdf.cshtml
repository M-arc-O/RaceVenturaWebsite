<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Team registration codes information for @Model.Name</title>
</head>
<body>
    <h1>Teams information for @Model.Name</h1>
    @foreach (var team in Model.Teams)
    {
        <p>
            <h3>@Model.Name - Team @team.Name (@team.Number)</h3>
            <img class="qrcode" src="@String.Format("data:image/png;base64,{0}", Convert.ToBase64String(team.QrCodeArray))" height="100" width="100" />
            <div style="height: 100px">This QR code is used to register your phone to the @Model.Name. The maximum team size for the @Model.Name is @Model.MaximumTeamSize, this means you can only register @Model.MaximumTeamSize phones. Be sure that these are the right phones because this cannot be undone! To register your phone to the @Model.Name download the Race Ventura app for the app/play store.</div>
        </p>
    }
</body>
</html>